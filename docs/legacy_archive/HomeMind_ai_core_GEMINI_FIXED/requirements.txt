# ============================================================
# HomeMind -- combined dependencies
# (ai_core orchestration + agentic pipeline, and the RAG /
#  Knowledge Agent module)
# ============================================================

# --- LangGraph orchestration -----------------------------------------
langgraph==0.2.74
langgraph-checkpoint==2.0.24
langgraph-checkpoint-postgres==2.0.24
langgraph-checkpoint-sqlite==2.0.6

# --- LLM client (Gemini via LangChain) -- used by ai_core agents ------
# Pinned to the last 2.x line release: it's still on langchain-core ^0.3.x,
# which is what langgraph==0.2.74 / langgraph-checkpoint==2.0.24 require.
# langchain-google-genai>=4.0.0 rewrote the package around the new unified
# google-genai SDK but bumped its floor to langchain-core>=1.2.5 -- taking
# that would break the pinned LangGraph/checkpointer stack above, so we
# stay on 2.x. The model string passed at call time (see llm/models.py)
# is what actually matters for Gemini 3.x support, not this package version.
langchain-google-genai==2.0.9
langchain-core==0.3.37

# --- Structured output / config schemas --------------------------------
pydantic==2.9.2
pydantic-settings==2.6.1

# --- Postgres driver (needed by langgraph-checkpoint-postgres in prod) --
psycopg[binary]==3.2.3

# --- pgvector support (if memory/knowledge stores use it directly) ------
pgvector==0.3.6

# --- RAG / Knowledge Agent module (agents/knowledge_agent, rag/) --------
python-dotenv==1.0.1
langchain-text-splitters==0.3.2
sentence-transformers==3.3.1
huggingface-hub==0.26.2
faiss-cpu==1.9.0
PyPDF2==3.0.1
python-docx==1.1.2
tiktoken==0.8.0
numpy==1.26.4

# --- Testing -------------------------------------------------------------
pytest==8.3.3
pytest-mock==3.14.0
