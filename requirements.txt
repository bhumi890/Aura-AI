# ============================================================
# MindMate AI — Combined Dependencies
# Backend (FastAPI) + AI Core (LangGraph + Gemini)
# ============================================================

# --- setuptools (needed for some packages on Python 3.13+) ---
setuptools

# --- FastAPI & Server ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0

# --- Database ---
sqlalchemy>=2.0.35
aiosqlite>=0.20.0

# --- Validation & Config ---
pydantic>=2.9.2
pydantic-settings>=2.5.0
python-dotenv>=1.0.1

# --- Voice - Speech to Text (OPTIONAL — browser STT is used instead) ---
# Uncomment if you need server-side Whisper transcription:
# openai-whisper==20231117
# openai==1.50.0

# --- Voice - Text to Speech (OPTIONAL — browser TTS is used instead) ---
# Uncomment if you need server-side gTTS:
# gTTS==2.5.1

# --- Audio Processing (OPTIONAL — only needed with server-side voice) ---
# pydub==0.25.1

# --- Auth Utilities ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
httpx>=0.27.0

# --- LangGraph orchestration (AI Core) ---
langgraph>=0.2.74
langgraph-checkpoint>=2.0.24
langgraph-checkpoint-postgres>=2.0.24
langgraph-checkpoint-sqlite>=2.0.6

# --- LLM client (Gemini via LangChain) ---
langchain-google-genai>=2.0.9
langchain-core>=0.3.37

# --- Postgres driver (for production checkpointing) ---
psycopg[binary]>=3.2.10

# --- RAG / Knowledge Agent ---
langchain-text-splitters>=0.3.2
sentence-transformers>=3.3.1
huggingface-hub>=0.26.2
faiss-cpu>=1.13.0
PyPDF2>=3.0.1
python-docx>=1.1.2
tiktoken>=0.8.0
numpy>=1.26.4

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0
